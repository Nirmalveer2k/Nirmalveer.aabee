import React from "react";

const Loading = () => {
  return (
    <main className="flex h-screen w-screen items-center justify-center bg-[#0a0a0a] text-9xl uppercase text-white">
      loading
    </main>
  );
};

export default Loading;
